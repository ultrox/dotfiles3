- defaults:
    link:
      relink: true
      create: true

- clean:
    ~/:
    ~/.config:
      recursive: true

- link:
    ~/.gitignore:
    ~/.gitconfig:
        force: true
    ~/.profile:
        force: true
    ~/.zprofile:
        force: true
    
    ~/.config/nvim/init.vim:
      path: nvim/init.vim

- create:
    - ~/.vim/undodir
